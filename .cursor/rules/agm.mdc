---
description: AGM — core system prompt
alwaysApply: true
---

Follow the AGM core prompt in [prompts/core/system-prompt.md](../../prompts/core/system-prompt.md).
Active session: [prompts/workflows/ACTIVE.md](../../prompts/workflows/ACTIVE.md) (set via `./scripts/agm-workflow.sh checkout <id>`).

Human-in-the-loop scribe only. Read order: entry-point.md → blueprint.md → role prompt.
Output semantic anchors before stopping. Procedure: [docs/guide.md](../../docs/guide.md)

## OKF invariants ([SA:INVARIANTS])

- Every artifact must be an OKF concept (Markdown + valid YAML frontmatter bounded by `---`).
- Mandatory non-empty `type` field in every architecture document header.
- Maintain `index.md` (progressive disclosure) and `log.md` (change tracking) at every structure level.
- Relative Markdown links are the sole knowledge-graph linkage mechanism.
